name: Docker

on:
  push:
    branches:
      - master
      - dev
  
  repository_dispatch:
    types: [dev-db-updated]

env:
  IMAGE_NAME: walld_aku_aku
  EVENT: ${{ github.event_name }}
  REF: ${{ github.ref }}
  VERSION: $(echo $REF | sed -e 's,.*/\(.*\),\1,')

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        if: github.event_name == 'repository_dispatch'
        with:
          ref: refs/heads/dev

      - uses: actions/checkout@v2
        if: github.event_name == 'push'
        with:
          ref: ${{ github.ref }}

      - name: RETRIVE_VERSION
        run: |
          LL=$(echo ${{ github.ref }} | sed -e 's,.*/\(.*\),\1,')
          echo ::set-env name=VERSION::$LL
          echo $VERSION
          sleep 2

      - name: Build image
        run: |
          docker build . --file Dockerfile --tag $IMAGE_NAME
          
      - name: Log into registry
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u kz159 --password-stdin

      - name: Push image
        run: |
          IMAGE_ID=kz159/$IMAGE_NAME
          # Change all uppercase to lowercase
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')
          echo VERSION $VERSION
          if [ "$EVENT" = "repository_dispatch" ]
          then
            VERSION="latest-dev"
          elif [ $VERSION = "dev" ]
          then
            VERSION="latest-dev"
          else
            VERSION='latest'
          fi
          
          echo IMAGE_ID=$IMAGE_ID
          echo VERSION=$VERSION
          
          docker tag $IMAGE_NAME $IMAGE_ID:$VERSION
          docker push $IMAGE_ID:$VERSION
